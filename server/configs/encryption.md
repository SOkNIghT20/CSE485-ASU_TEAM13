# Instructions
Place the credentials.txt into the folder.

If you would like to encrypt the config, execute under the same folder:

`node encrypt_credentials.js`

The credential.txt file can now be deleted from the folder.