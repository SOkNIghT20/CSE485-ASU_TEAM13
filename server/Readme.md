# Instructions for setting up the express server:
1. run `cd server`
2. run `npm install`
3. run `npm run start` for backend production or `npm run debug` for backend local 

The API is now running on http://localhost:3000, and can be connected to by the frontend.